using Domain.Ports.Driving;
using SqlServerAdapter;
using SqlServerAdapter.Data;

namespace SqlAdapter.Tests
{
    public class RutasRepositoryUniTest
    {
     /*   [Fact]
        public void NumeroProgramasPorRuta_RutaValida_ReturnsOk()
        {
            var pc = new RutaRepository(new RutaContext());


            var x = pc.ObtenerNumeroProgramasPorRuta(57);

            if (x == 0)
            {
            }
        }

        [Fact]
        public void ObtenerRutasPorRegionSsf_TipoAereoValidSsf_ReturnsOk()
        {
            var pc = new RutaRepository(new RutaContext());


            var x = pc.ObtenerRutasPorRegionSsf("AEREO", 6);

            if (x.Count == 0)
            {
            }
        }

        [Fact]
        public void ObtenerRutasPorRegionSsf_TipoTerrestreValidSsf_ReturnsOk()
        {
            var pc = new RutaRepository(new RutaContext());


            var x = pc.ObtenerRutasPorRegionSsf("TERRESTRE", 6);

            if (x.Count == 0)
            {
            }
        }

        [Fact]
        public void ObtenerRutasPorRegionMilitar_TipoTerrestreValidRegion_ReturnsOk()
        {
            var pc = new RutaRepository(new RutaContext());


            var x = pc.ObtenerRutasPorRegionMilitar("TERRESTRE", "6");

            if (x.Count == 0)
            {
            }
        }

        [Fact]
        public void ObtenerRutasPorCombinacionFiltros_TipoTerrestreValidRegion_ReturnsOk()
        {
            var pc = new RutaRepository(new RutaContext());

            
            var x = pc.ObtenerRutasPorCombinacionFiltros("TERRESTRE", "PO_A3190");

            if (x.Count == 0)
            {
            }
        }

        [Fact]
        public void ObtenerPropuestasPorRegionMilitarAndRegionSsf_TipoTerrestreConRegionUsuario_ReturnsOk()
        {
            var pc = new RutaRepository(new RutaContext());


            var x = pc.ObtenerPropuestasPorRegionMilitarAndRegionSsf("TERRESTRE", "6",6);

            if (x.Count == 0)
            {
            }
        }

        [Fact]
        public void ObtenerPropuestasPorCombinacionFiltrosConRegionSsf_TipoAereoConRegionUsuario_ReturnsOk()
        {
            var pc = new RutaRepository(new RutaContext());


            var x = pc.ObtenerPropuestasPorCombinacionFiltrosConRegionSsf("AEREO", "PO_93280", 3);

            if (x.Count == 0)
            {
            }
        }*/
    }
}
